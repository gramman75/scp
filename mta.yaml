_schema-version: "3.1"
ID: final1-cds
description: Generated by cds-services-archetype
version: 1.0.0
modules:
- name: final1-db-dev
  type: hdb
  path: db
  requires:
  - name: final1-hdi-container
  parameters:
    app-name: final1-db-dev
- name: final1-cds-srv
  type: java
  path: srv
  requires:
  - name: final1-hdi-container
  provides:
  - name: srv-api
    properties:
      srv-url: ${default-url}
  build-parameters:
    build-result: target/*.[wj]ar
    builder: custom
    commands:
    - mvn clean package
- name: final1-destination-content
  type: com.sap.application.content
  requires:
  - name: uaa_final1
    parameters:
      service-key:
        name: uaa_final1-key
  - name: final1_html_repo_host
    parameters:
      service-key:
        name: final1_html_repo_host-key
  - name: final1-destination-service
    parameters:
      content-target: true
  parameters:
    content:
      subaccount:
        destinations:
        - Name: final1_final1_html_repo_host
          ServiceInstanceName: final1-html5-app-host-service
          ServiceKeyName: final1_html_repo_host-key
          sap.cloud.service: final1
        - Authentication: OAuth2UserTokenExchange
          Name: final1_uaa_final1
          ServiceInstanceName: final1-xsuaa-service
          ServiceKeyName: uaa_final1-key
          sap.cloud.service: final1
        existing_destinations_policy: update
  build-parameters:
    no-source: true
- name: final1_ui_deployer
  type: com.sap.application.content
  path: app
  requires:
  - name: final1_html_repo_host
    parameters:
      content-target: true
  build-parameters:
    build-result: resources
    requires:
    - artifacts:
      - final1-fiori-content.zip
      name: final1-fiori
      target-path: resources/
- name: final1-fiori
  type: html5
  path: app/final1-fiori
  build-parameters:
    builder: custom
    commands:
    - npm run build
    supported-platforms: []
resources:
- name: final1-hdi-container
  type: com.sap.xs.hdi-container
  parameters:
    config:
      makeUniqueName: false
    service: hana
    service-plan: hdi-shared
  properties:
    hdi-service-name: ${service-name}
- name: final1-destination-service
  type: org.cloudfoundry.managed-service
  parameters:
    config:
      init_data:
        subaccount:
          destinations:
          - Authentication: NoAuthentication
            HTML5.ForwardAuthToken: true
            Name: srv-api
            ProxyType: Internet
            Type: HTTP
            URL: ~{srv-api/srv-url}
          existing_destinations_policy: update
    service: destination
    service-name: final1-destination-service
    service-plan: lite
  requires:
  - name: srv-api
- name: final1_html_repo_host
  type: org.cloudfoundry.managed-service
  parameters:
    service: html5-apps-repo
    service-name: final1-html5-app-host-service
    service-plan: app-host
- name: uaa_final1
  type: org.cloudfoundry.managed-service
  parameters:
    path: ./xs-security.json
    service: xsuaa
    service-name: final1-xsuaa-service
    service-plan: application
parameters:
  enable-parallel-deployments: true
build-parameters:
  before-all:
  - builder: custom
    commands:
    - npm install
